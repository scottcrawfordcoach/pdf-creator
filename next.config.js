/** @type {import('next').NextConfig} */
const nextConfig = {
  // Python serverless functions live in the top-level /api directory and are
  // handled by Vercel's Python runtime – no special Next.js config needed.
}

module.exports = nextConfig
